[build-system]
requires = ["scikit-build-core", "pybind11"]
build-backend = "scikit_build_core.build"

[project]
name = "XTED_CPU"
version = "0.1.0"
requires-python = ">=3.8"

[tool.scikit-build]
wheel.packages = ["python/xted_cpu"]

[tool.pytest.ini_options]
addopts = "--import-mode=importlib"